{
  "name": "e-commerce-smoke-suite",
  "version": "1.0.0",
  "private": true,
  "description": "A Playwright smoke test suite demonstrating optimization and parallelism for a virtual e-commerce store.",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:report": "npx playwright show-report",
    "test:debug": "npx playwright test --debug",
    "test:ci": "npx playwright install --with-deps && npx playwright test --reporter=html"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^20.14.0",
    "typescript": "^5.6.0"
  }
}
